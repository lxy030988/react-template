# Quicklink 预加载功能使用指南

## ✅ 已完成的集成

### 1. 安装的依赖
- `quicklink@3.0.1` - 智能预加载库

### 2. 创建的文件

#### [`src/hooks/useQuicklink.ts`](file:///Users/lxy/Desktop/lxy030988/react-template/src/hooks/useQuicklink.ts)
自定义 Hook，提供 Quicklink 预加载功能，包括：
- ✅ 网络状态检测（慢速网络下自动禁用）
- ✅ 省流模式检测
- ✅ 可配置的预加载策略
- ✅ 完整的 TypeScript 类型支持

#### [`src/types/quicklink.d.ts`](file:///Users/lxy/Desktop/lxy030988/react-template/src/types/quicklink.d.ts)
TypeScript 类型声明文件

#### [`src/layouts/Layout.tsx`](file:///Users/lxy/Desktop/lxy030988/react-template/src/layouts/Layout.tsx)
主布局组件，已集成 Quicklink

---

## 🚀 如何验证 Quicklink 是否工作

### 方法 1：使用 Chrome DevTools（推荐）

1. **启动开发服务器**（已启动）
   ```bash
   pnpm run server
   # 访问 http://localhost:3000
   ```

2. **打开 Chrome DevTools**
   - 按 `F12` 或右键 → 检查
   - 切换到 **Network** 面板

3. **清空网络记录**
   - 点击 🚫 清空按钮

4. **访问首页并观察**
   - 刷新页面
   - 在 Console 标签查看日志：
     ```
     [Quicklink] 预加载已启用 {limit: 3, timeout: 2000, ...}
     ```

5. **检查预加载请求**
   在 Network 面板中：
   - 点击 "Type" 列排序
   - 查找 **prefetch** 类型的请求
   - 或者查找 **Priority** 为 `Lowest` 的 JS 文件

   **预期结果**：
   ```
   Name                          Type      Priority    Size
   -----------------------------------------------------------
   493.6e2fb.bundle.js          prefetch   Lowest      18 KB
   705.4ef21.bundle.js          prefetch   Lowest      7.4 KB
   ```

6. **点击链接验证效果**
   - 点击任意导航链接（如 About、Demo）
   - 页面应该**立即**渲染，无需等待
   - Network 面板显示资源来自 `(prefetch cache)`

### 方法 2：查看控制台日志

打开浏览器控制台（Console），你应该看到：

```
[Quicklink] 预加载已启用 {limit: 3, timeout: 2000, throttle: 0, threshold: 0, priority: false}
```

### 方法 3：使用 Performance 面板

1. 打开 DevTools → **Performance** 面板
2. 点击 **Record** 录制
3. 访问首页，然后停止录制
4. 在时间线中查找 `prefetch` 相关的请求
5. 应该能看到多个"低优先级"的资源加载

---

## 📊 性能对比测试

### 测试步骤

1. **禁用 Quicklink 测试**
   ```tsx
   // src/layouts/Layout.tsx
   // 注释掉 useQuicklink
   // useQuicklink({...})
   ```

2. **记录首次导航时间**
   - 刷新首页（禁用缓存）
   - 点击 "About" 链接
   - 记录页面加载时间

3. **启用 Quicklink 测试**
   ```tsx
   // 取消注释
   useQuicklink({
     limit: 3,
     timeout: 2000,
     throttle: 0,
     threshold: 0,
   })
   ```

4. **再次测试**
   - 刷新首页（禁用缓存）
   - 滚动页面（确保链接可见）
   - 等待 1-2 秒（让预加载完成）
   - 点击 "About" 链接
   - 记录页面加载时间

**预期结果**：
- 无 Quicklink：页面加载需要 200-500ms
- 有 Quicklink：页面**立即**显示（< 50ms）

---

## ⚙️ 配置选项说明

当前配置：

```tsx
useQuicklink({
  limit: 3,       // 最多同时预加载 3 个资源
  timeout: 2000,  // 2 秒超时
  throttle: 0,    // 不节流，立即预加载
  threshold: 0,   // 链接进入视口立即触发
})
```

### 可调整的配置

| 参数 | 默认值 | 说明 | 推荐值 |
|------|--------|------|--------|
| `limit` | 3 | 最大并发预加载数 | 2-5（取决于网络） |
| `timeout` | 2000 | 预加载超时时间（ms） | 2000-5000 |
| `throttle` | 0 | 节流延迟（ms） | 0-500 |
| `threshold` | 0 | IntersectionObserver 阈值 | 0-0.5 |
| `priority` | false | 是否高优先级 | false |

### 针对不同场景的配置

#### 移动端优化（已内置）
```tsx
// Hook 会自动检测：
// - 慢速网络（2G/slow-2g）→ 禁用
// - 省流模式 → 禁用
```

#### 激进预加载
```tsx
useQuicklink({
  limit: 5,
  timeout: 3000,
  throttle: 0,
})
```

#### 保守预加载
```tsx
useQuicklink({
  limit: 2,
  timeout: 1000,
  throttle: 500,
  threshold: 0.5, // 链接50%可见时才预加载
})
```

---

## 🐛 常见问题

### 1. 看不到预加载请求

**可能原因**：
- 页面链接不在可视区域
- 网络速度太快，预加载瞬间完成
- 浏览器缓存了资源

**解决方法**：
- 打开 DevTools Network 面板
- 勾选 "Disable cache"
- 刷新页面

### 2. 控制台没有日志

**可能原因**：
- 被慢速网络或省流模式禁用了

**解决方法**：
- 检查 Console 是否有 "检测到慢速网络" 的日志
- 在非移动网络环境测试

### 3. 预加载没有提升性能

**可能原因**：
- 资源已被浏览器缓存
- 预加载时间不足

**解决方法**：
- 禁用浏览器缓存测试
- 增加 `timeout` 时间
- 确保链接在视口内停留足够时间

---

## 📈 监控和调试

### 查看预加载效果

在 Console 中运行：

```javascript
// 查看性能指标
performance.getEntriesByType('resource')
  .filter(r => r.name.includes('bundle.js'))
  .map(r => ({
    name: r.name.split('/').pop(),
    duration: r.duration,
    type: r.initiatorType
  }))
```

### 检查Intersection Observer

```javascript
// 查看 Quicklink 监听的元素
document.querySelectorAll('a[href^="/"]')
// 应该能看到所有内部链接
```

---

## 🎯 下一步：实施 Guess.js

Quicklink 只是第一步，**Guess.js** 可以进一步优化：

### Guess.js vs Quicklink

| 特性 | Quicklink | Guess.js |
|------|-----------|----------|
| 预加载策略 | 可视区域所有链接 | **智能预测**用户最可能点击的链接 |
| 数据依赖 | 无 | 需要 Google Analytics 数据 |
| 准确度 | - | 基于机器学习预测 |
| 网络消耗 | 相对较高 | **更低**（只加载预测的） |

**何时实施 Guess.js**：
- ✅ 有 Google Analytics
- ✅ 有至少 1-2 周的访问数据
- ✅ 想进一步优化网络使用

---

## 📝 总结

✅ **已完成**：
1. 安装 `quicklink` 依赖
2. 创建 `useQuicklink` Hook
3. 集成到主布局组件
4. 添加网络检测和移动端优化
5. 配置最佳实践参数

🎯 **下一步**：
- 在浏览器中验证预加载效果
- 根据实际情况调整配置
- 准备实施 Guess.js（如果需要）

**测试地址**：http://localhost:3000
